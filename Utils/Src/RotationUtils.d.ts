export declare namespace Facing {
  function getClockWise(a1: number): number;
  function getOpposite(a1: number): number;
  function getCounterClockWise(a1: number): number;
}

export declare namespace RotationUtils {
  function rotate(a1: number, a2: number): number;
}